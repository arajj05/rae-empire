const express = require("express");
const router = express.Router();

router.get("/session-scan", (req, res) => {
  console.log("🌐 Rae is scanning for active sessions...");
  res.status(200).json({ message: "Session Scanner: Monitoring active logins." });
});

module.exports = router;
